# Test

This directory groups manual integration test programs and GoogleTest unit tests.

## Subdirectories

- `integration_test/` contains the tests which require human oversight to determine if they pass
- `unit_test/` are the automated tests
